package version

var (
	// BuildTime is a label when build
	BuildTime = "unset"
	// Commit is a label which git version is build
	Commit = "unset"
	// Release is a semantic version
	Release = "unset"
)
